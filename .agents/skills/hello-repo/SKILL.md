---
name: hello-repo
description: Minimal repo-scoped skill example discovered from .agents/skills without plugin install.
---

# hello-repo

Confirm this repository's Codex layout (plugins marketplace + `.codex/agents`) and summarize available project subagents from `.codex/config.toml`.
