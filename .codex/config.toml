# Project-scoped Codex agents (trust this repo to load).
# Pattern aligned with https://github.com/proflead/codex-agents-library

[features]
multi_agent_v2 = true

[agents]
enabled = true
max_threads = 6
max_depth = 1
interrupt_message = true
